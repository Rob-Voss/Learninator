export default class Point {
    /**
     * Simple Point
     * @name Point
     * @constructor
     *
     * @param {number} x
     * @param {number} y
     * @returns {Point}
     */
    constructor(x, y) {
        this.x = x;
        this.y = y;

        return this;
    }
}